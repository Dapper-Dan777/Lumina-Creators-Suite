{
  "name": "lumina-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:seed": "node -e \"import('./seed.js').then(m => m.seedDatabaseIfEmpty())\""
  },
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6",
    "prisma": "^6.9.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}